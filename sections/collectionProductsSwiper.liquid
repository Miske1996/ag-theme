<link
  rel="stylesheet"
  href="{{ 'collectionProductsSwiper.css' | asset_url }}"
  media="print"
  onload="this.media='all'; this.onload=null;"
>
<link
    rel="stylesheet"
    href="{{ 'productCard.css' | asset_url }}"
    media="print"
    onload="this.media='all'; this.onload=null;"
    >
<collection-products-swiper>
  <section class="collection-products-swiper">
    {% render 'collectionSwiperHeader', collection: section.settings.collection, header: section.settings.heading %}
    <div class="collection-products-swiper__products swiper">
      <ul class="collection-products-swiper__wrapper swiper-wrapper">
        {% for product in section.settings.collection.products %}
          {% render 'productCard', product: product, class: 'swiper-slide' %}
        {% endfor %}
      </ul>
    </div>
  </section>
</collection-products-swiper>
<script src="{{ "CollectionProductsSwiper.js" | asset_url }}" defer></script>
<script src="{{ "ProductCard.js" | asset_url }}" defer></script>

{% schema %}
{
  "name": "Collection swiper",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Title heading",
      "default": "🌟 Trending"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    }
  ],
  "presets": [
    {
      "name": "Collection swiper"
    }
  ]
}
{% endschema %}
